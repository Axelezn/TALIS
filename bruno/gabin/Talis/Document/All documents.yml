info:
  name: All documents
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_URL}}/api/documents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
