info:
  name: Single document
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_URL}}/api/documents/{{id_document}}"
  auth: inherit

runtime:
  variables:
    - name: id_document
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
