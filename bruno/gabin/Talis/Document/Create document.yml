info:
  name: Create document
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_URL}}/api/documents"
  body:
    type: json
    data: |-
      {
      "nom": "cv-jean-dupont.pdf",
      "path": "/uploads/cv-jean-dupont.pdf"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
