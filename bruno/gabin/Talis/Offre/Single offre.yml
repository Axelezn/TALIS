info:
  name: Single offre
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_URL}}/api/offres/{{id_offre}}"
  auth: inherit

runtime:
  variables:
    - name: id_offre
      value: "6"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
