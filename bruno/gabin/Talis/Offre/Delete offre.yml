info:
  name: Delete offre
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_URL}}/api/offres/{{id_offre}}"
  auth: inherit

runtime:
  variables:
    - name: id_offre
      value: "5"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
