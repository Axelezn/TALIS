info:
  name: All offres
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_URL}}/api/offres"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
