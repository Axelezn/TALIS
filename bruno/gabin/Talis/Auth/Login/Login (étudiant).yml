info:
  name: Login (étudiant)
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_URL}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "role": "etudiant",
        "mail": "alice@example.com",
        "password": "motdepasse123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
