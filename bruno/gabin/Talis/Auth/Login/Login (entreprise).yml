info:
  name: Login (entreprise)
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_URL}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "role": "entreprise",
        "mail": "recruteur@example.com",
        "password": "motdepasse123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
