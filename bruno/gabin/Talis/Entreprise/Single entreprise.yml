info:
  name: Single entreprise
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_URL}}/api/entreprises/{{id_entreprise}}"
  auth: inherit

runtime:
  variables:
    - name: id_entreprise
      value: "2"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
