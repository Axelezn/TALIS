info:
  name: Create demande
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_URL}}/api/demandes"
  body:
    type: json
    data: |-
      {
      "id_user": 1,
      "id_offre": 1,
      "demande": "Je suis interesse par cette offre."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
