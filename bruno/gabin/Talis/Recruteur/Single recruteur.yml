info:
  name: Single recruteur
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_URL}}/api/recruteurs/{{id_recruteur}}"
  auth: inherit

runtime:
  variables:
    - name: id_recruteur
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
