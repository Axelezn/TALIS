info:
  name: All utilisateurs
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_URL}}/api/utilisateurs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
