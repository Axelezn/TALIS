info:
  name: Single utilisateur
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_URL}}/api/utilisateurs/{{id_utilisateur}}"
  auth: inherit

runtime:
  variables:
    - name: id_utilisateur
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
